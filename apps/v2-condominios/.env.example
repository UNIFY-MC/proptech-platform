# V1 Core Hub (hkmvszkpxjbxmnixzqbl) — schema v2_condominios + core + system
# A chave anon é pública (read-only por RLS), mas mantém-se em env para clareza.
VITE_SUPABASE_ANON_KEY=eyJ...substitute-with-real-anon-key

# (opcional) V2 Condo Hub legacy (eozklslwfaqujaijvdnl) — produção prataowners.pt
# READ-ONLY. Permite validar dados reais (97 fracções, 2733 documentos, etc)
# enquanto o schema v2_condominios no V1 Core Hub ainda está vazio.
# Anon vê apenas 8 tabelas RLS-off; o resto requer login user real do utilizadores_portal.
VITE_SUPABASE_V2_LEGACY_ANON_KEY=eyJ...substitute-or-leave-empty
