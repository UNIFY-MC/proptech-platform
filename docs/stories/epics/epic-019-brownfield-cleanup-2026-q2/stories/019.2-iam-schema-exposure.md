# Story 019.2 — Verificar e corrigir exposição PostgREST de schemas custom

**Story ID:** 019.2
**Epic:** epic-019-brownfield-cleanup-2026-q2
**Covers debts:** DB-016 (Medium), DB-018 (Medium)
**Effort:** S
**Sequence:** Fase A
**Status:** InReview

## As a

Mário (operador) e qualquer agente que dependa de RPCs ou queries REST cross-schema

## I want

Confirmar — e corrigir, se necessário — que os schemas `iam`, `marketing`, `v2_new` (e `growth` se aplicável) estão expostos via PostgREST no V1 Core Hub

## So that

RPCs como `iam.has_permission()`, `iam.get_my_permissions()`, `iam.portal_token_login()` funcionem via REST para frontend; falhas silenciosas tipo `PGRST106 Invalid schema` ou retornos 404 deixem de mascarar bugs cross-vertical; tabelas de referência sem PII (ex: `core.codigos_postais`) sejam consultáveis por `authenticated`.

## Acceptance Criteria

1. **AC-1** — Diagnóstico read-only executado em V1:
   ```sql
   SELECT rolconfig FROM pg_roles WHERE rolname = 'authenticator';
   ```
   Resultado documentado no commit.
2. **AC-2** — Se `pgrst.db_schemas` não contém `iam`, `marketing`, `v2_new`, é executado o procedimento canónico do ADR-V2-002:
   ```sql
   ALTER ROLE authenticator SET pgrst.db_schemas =
     'public, graphql_public, core, system, iam, marketing, v2_condominios,
      v3_seguros, v4_energia, v5_manutencao, v1_owners_club, v2_new, growth';
   NOTIFY pgrst, 'reload config';
   ```
3. **AC-3** — Smoke test REST: chamada a `iam.get_my_permissions()` via PostgREST retorna 200 (ou 401 com JWT inválido), não 404 nem PGRST106.
4. **AC-4** — `core.codigos_postais` (205 817 rows) recebe policy de leitura pública: `CREATE POLICY codigos_postais_read ON core.codigos_postais FOR SELECT TO authenticated USING (true);`
5. **AC-5** — Smoke test: query a `core.codigos_postais` por código postal específico retorna >=1 row para utilizador `authenticated`.
6. **AC-6** — Documentação actualizada em CLAUDE.md secção "Exposição de schemas custom ao PostgREST" — confirmar que lista final está em git.

## Tasks

- [x] Executar diagnóstico SQL read-only via Supabase CLI (V1 + V2)
- [x] Comparar resultado com lista esperada (CLAUDE.md root)
- [x] Criar migration `20260523191721_postgrest_expose_custom_schemas.sql` (NÃO aplicada — aguarda aprovação)
- [x] Criar migration `20260523191722_codigos_postais_public_read.sql` para DB-018 (NÃO aplicada — aguarda aprovação)
- [x] Documentar audit completo em `docs/database/postgrest-exposure-audit-2026-05-23.md`
- [x] Documentar V2 read-only status em `docs/database/v2-postgrest-status-2026-05-23.md`
- [ ] **Mário aplicar migrations via SQL Editor / `supabase db push`** (post-handoff)
- [ ] **Smoke test cURL via REST após aplicação** (post-handoff)
- [ ] **Smoke test query `core.codigos_postais` via REST com `authenticated` JWT** (post-handoff)
- [ ] **Actualizar CLAUDE.md root com lista canónica final** (post-handoff — feito por @devops na consolidação)

## Dependencies

- Nenhuma — **pré-requisito hard** de todas as outras stories que dependem de REST

## Risk & Rollback

- Operação é low-risk — ALTER ROLE é idempotente, NOTIFY é safe
- Rollback exposure: re-executar `ALTER ROLE` com lista anterior (documentado em cada migration header e em audit doc)
- Rollback policy: `DROP POLICY codigos_postais_public_read ON core.codigos_postais;`
- Policy de leitura em `codigos_postais` é additive — sem regressão

## References

- Assessment: §DB-016, §DB-018, §"Pré-requisitos Absolutos" (DB-016 PRIMEIRO de todos)
- ADR-V2-002 (postgrest schema exposure)
- CLAUDE.md root §"Exposição de schemas custom ao PostgREST"

---

## Dev Notes

### Diagnóstico executado (2026-05-23)

**V1 Core Hub (`hkmvszkpxjbxmnixzqbl`):**
- `pgrst.db_schemas` actual: `public, graphql_public, core, system, iam, growth, v2_condominios, v3_seguros, v4_energia, v5_manutencao, v1_owners_club`
- Schemas custom existentes: `core (38 tables), cron, growth (9), iam (6), marketing (13), system (52), v1_owners_club (3), v2_condominios (66), v2_new (6), v3_seguros (4), v4_energia (10), v5_manutencao (46)`
- **Gap identificado:** `marketing` e `v2_new` existem mas NÃO estão expostos. Frontend hits `PGRST106 Invalid schema`.

**V2 Condo Hub (`eozklslwfaqujaijvdnl`):**
- `pgrst.db_schemas` não definido (default `public` only)
- Zero schemas custom — arquitectura monolítica em `public` (correcto para o desenho actual)
- **Nenhuma acção necessária em V2.** Documentado em `v2-postgrest-status-2026-05-23.md`.

### DB-018 — `core.codigos_postais`

- Tabela existe com 205 817 linhas (dataset CTT completo)
- `GRANT SELECT TO authenticated, anon` já presente ✅
- RLS ENABLED ✅
- **ZERO policies** ❌ → default-deny silencioso
- Fix: policy `USING (true)` para SELECT (dados públicos, zero PII)

### Decisões autonomous

| Decisão | Justificação |
|---------|--------------|
| Manter `v1_owners_club` (não renomear para `v10_owners_club`) | Rename é scope de story 019.14 |
| Adicionar BOTH `marketing` AND `v2_new` (story listou só `v2_new` como prioridade) | Ambos existem com conteúdo substantivo (13 e 6 tabelas) e referenciados em CLAUDE.md ADR-015 |
| `iam.get_my_permissions()` referido em AC-3 NÃO existe na DB | Funções equivalentes (`iam.has_permission()`, `iam.user_can()`) existem. Documentado em audit; criar `get_my_permissions` é out-of-scope desta story |
| NÃO aplicar migrations via `supabase db push` | Spawn prompt explicitamente diz: criar ficheiros, aguardar aprovação do Mário |
| Excluir `cron` schema da exposure | Schema interno Supabase, não destinado a REST |

### Smoke tests pós-aplicação (Mário deve executar)

```bash
# 1. Verify exposure
psql $V1_DB_URL -c "SELECT rolconfig FROM pg_roles WHERE rolname = 'authenticator';"

# 2. REST endpoint for marketing schema (deve retornar 200 ou 401, não 404)
curl -s -H "apikey: $ANON_KEY" -H "Authorization: Bearer $ANON_KEY" \
  "https://hkmvszkpxjbxmnixzqbl.supabase.co/rest/v1/?select=*" | jq '.definitions | keys | .[] | select(startswith("marketing"))'

# 3. codigos_postais read
curl -s -H "apikey: $ANON_KEY" -H "Authorization: Bearer $ANON_KEY" \
  "https://hkmvszkpxjbxmnixzqbl.supabase.co/rest/v1/codigos_postais?codigo=eq.1000-001&select=*&limit=1"
```

### Pendente para @devops (após aprovação Mário)

1. Aplicar `20260523191721_postgrest_expose_custom_schemas.sql` em V1
2. Aplicar `20260523191722_codigos_postais_public_read.sql` em V1
3. Executar smoke tests acima
4. Actualizar CLAUDE.md root §"Exposição de schemas custom ao PostgREST" com a lista final aplicada
5. Confirmar AC-3 e AC-5

---

## File List

**Created:**
- `supabase/migrations/20260523191721_postgrest_expose_custom_schemas.sql` (V1 — NOT applied)
- `supabase/migrations/20260523191722_codigos_postais_public_read.sql` (V1 — NOT applied)
- `docs/database/postgrest-exposure-audit-2026-05-23.md`
- `docs/database/v2-postgrest-status-2026-05-23.md`

**Modified:**
- `docs/stories/epics/epic-019-brownfield-cleanup-2026-q2/stories/019.2-iam-schema-exposure.md` (this file — status, tasks, dev notes, file list, change log)

**Pending modification (post-approval):**
- `CLAUDE.md` raiz §"Exposição de schemas custom ao PostgREST" — actualizar lista canónica para reflectir o que foi aplicado em produção

---

## Dev Agent Record

**Agent Model Used:** Opus 4.7 (1M context) via @data-engineer (Dara)
**Mode:** YOLO
**Execution Date:** 2026-05-23
**Branch:** `feat/db-016-postgrest-exposure`

### Debug Log References

- Audit doc: `docs/database/postgrest-exposure-audit-2026-05-23.md`
- V2 audit: `docs/database/v2-postgrest-status-2026-05-23.md`
- Supabase CLI used for read-only diagnostics via `supabase db query --linked`

### Completion Notes

- AC-1 ✅ Diagnóstico executado e documentado
- AC-2 ⏸️ Migration criada, NÃO aplicada (per spawn prompt — aguarda Mário)
- AC-3 ⏸️ Smoke test pendente (post-apply) + nota: `iam.get_my_permissions()` não existe; usar `iam.has_permission()` ou `iam.user_can()`
- AC-4 ⏸️ Migration criada, NÃO aplicada
- AC-5 ⏸️ Smoke test pendente (post-apply)
- AC-6 ⏸️ CLAUDE.md update pendente (post-apply) — @devops após confirmação de exposure

**Status final: InReview** — handed off to Mário/@devops para aplicar migrations e completar smoke tests.

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-05-23 | 0.1.0 | Story created (Draft) | @sm / Epic 019 planning |
| 2026-05-23 | 1.0.0 | Development started (YOLO mode) — Status: Draft → InProgress. Diagnostic V1+V2 complete, migrations + audit docs created. | @data-engineer (Dara) |
| 2026-05-23 | 1.0.1 | Development complete — Status: InProgress → InReview. Migrations NOT applied (awaiting Mário approval per spawn prompt). | @data-engineer (Dara) |
